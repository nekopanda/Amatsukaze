<UserControl x:Class="Amatsukaze.Views.SettingPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Amatsukaze.ViewModels" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance {x:Type vm:SettingViewModel}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="エンコーダ" VerticalAlignment="Top" Margin="0,7,0,0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="追加オプション" VerticalAlignment="Top" Margin="0,63,0,0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="一時フォルダ" VerticalAlignment="Top" Margin="0,123,0,0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="x264パス" VerticalAlignment="Top" Margin="0,151,0,0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="x265パス" VerticalAlignment="Top" Margin="0,179,0,0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="QSVEncパス" VerticalAlignment="Top" Margin="0,207,0,0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="Muxerパス" VerticalAlignment="Top" Margin="0,235,0,0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="TimelineEditorパス" VerticalAlignment="Top" Margin="0,263,0,0"/>

        <ComboBox Grid.Column="1" HorizontalAlignment="Left" Margin="10,4,0,0" VerticalAlignment="Top" Width="120" ItemsSource="{Binding EncoderList}" SelectedIndex="{Binding EncoderSelectedIndex, Mode=TwoWay}"/>
        <TextBox Grid.Column="1" Height="85" Margin="10,32,10,0" TextWrapping="Wrap" Text="{Binding Model.EncoderOption, Mode=TwoWay, UpdateSourceTrigger=Default}" VerticalAlignment="Top"/>
        <TextBox Grid.Column="1" Height="23" Margin="10,122,10,0" TextWrapping="Wrap" Text="{Binding Model.WorkPath, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox Grid.Column="1" Height="23" Margin="10,150,10,0" TextWrapping="Wrap" Text="{Binding Model.X264Path, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox Grid.Column="1" Height="23" Margin="10,178,10,0" TextWrapping="Wrap" Text="{Binding Model.X265Path, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox Grid.Column="1" Height="23" Margin="10,206,10,0" TextWrapping="Wrap" Text="{Binding Model.QSVEncPath, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox Grid.Column="1" Height="23" Margin="10,234,10,0" TextWrapping="Wrap" Text="{Binding Model.MuxerPath, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox Grid.Column="1" Height="23" Margin="10,262,10,0" TextWrapping="Wrap" Text="{Binding Model.TimelineEditorPath, Mode=TwoWay}" VerticalAlignment="Top"/>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" TextWrapping="Wrap"
                   Text="「適用」でサーバに反映。「更新」を押すと編集中の設定が失われるので注意"
                   VerticalAlignment="Top" Margin="0,7,0,0"/>
        
        <Button Content="適用" Grid.Column="1" Margin="0,5,9,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="60" Command="{Binding SendSettingCommand}"/>

    </Grid>
</UserControl>
